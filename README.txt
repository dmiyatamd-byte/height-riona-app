v3.3 フル統合版（安定・高級UI・縦軸100-190固定）
- 1) 身長予測：フィット/予測レンジ/保存
- 2) スポーツ貧血/リオナ：評価→服用中のみ改善予測→実測→再計算
- 3) アドバイス：運動/栄養/睡眠（スケール例・睡眠負債）
- 4) レポート：PDF簡易
- 縦軸は 100–190 cm 固定
- インデント安全（後付けパッチなし）

起動:
  pip install -r requirements.txt
  streamlit run app.py

注意:
- ADMIN_PASSPHRASE を必ず変更してください。


v3.4:
- 身長予測のフィードバック（希望身長差・受診推奨）を復活
- リオナの改善予測表示をメトリクス＋アラート＋モデル表記に変更（JSON直表示を廃止）


v3.5:
- タブを st.tabs から目立つ横並びナビ（radio）に変更し、ページ選択を維持して『戻れない』問題を回避
- リオナのフォロー入力でIDを空欄でも直前case_idを自動採用


v3.6:
- 睡眠：起床時刻デフォルト6:00、睡眠時間デフォルト8.0（0.25h刻み）、睡眠スコア表示
- フォロー入力：IDラベルを患者向けに分かりやすく変更（通常は空欄でOK）


v3.7:
- テスト段階：ワンタイムコードを廃止し、ID/パスワード方式（初回登録）へ
- データはユーザー単位（IDのハッシュ）で自動保存
- 次回起動時に前回データを自動復元（主要項目）


v4.0:
- 食事ログ（1日チェック）追加：目的×強度×競技×年齢×体重でPFC目標/総kcalを算出し、朝昼夕（給食は年齢で小/中自動）から達成度を表示


v1.4:
- 身長予測の『復元』を『記入データ読込』に変更
- 食事写真解析はOpenAIではなく外部API（MEAL_API_URL）へ送信
- トレーニング提案も外部API（TRAIN_API_URL）対応
- secrets.toml でURL設定


OpenAI直接利用（テスト用）
- app.py 冒頭の OPENAI_API_KEY を sk- ではじまるキーに置き換えてください。
- 画像解析/筋トレ提案/怪我推論はOpenAIで行います。
